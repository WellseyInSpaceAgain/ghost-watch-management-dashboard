# Copy to .env and fill in credentials from your separate Ghost Watch SSO registration.
# Never commit .env or paste its values into logs/chat.
Eve__ClientId=
Eve__ClientSecret=
Eve__CallbackUrl=http://localhost:8080/api/auth/eve/callback

# Host port only; if changed, update the callback above and the EVE registration too.
GHOST_WATCH_PORT=8080
